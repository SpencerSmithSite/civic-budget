<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <!-- CDK apps are plain console apps the CDK CLI runs (see cdk.json); the analyzers stay on. -->
    <RootNamespace>CivicBudget.Infra</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Amazon.CDK.Lib" />
    <PackageReference Include="Constructs" />
  </ItemGroup>
</Project>
