{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "CivicBudget is supplied by user-secrets locally (scripts/dev-setup.sh) and by AWS Secrets Manager in the cloud. Never put it here."
  }
}
